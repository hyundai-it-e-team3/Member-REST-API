<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.mycompany.memberAPI.dao.MemberDao">
	<select id="selectByMemberId" parameterType="String" resultType="member">
		SELECT *
		FROM member
		WHERE member_id = #{memberId}
	</select>

	<insert id="insertMember" parameterType="member">
		INSERT INTO member (member_id, password, name, nickname, email, tel, birthday, member_level, member_role, status, last_login_date)
		VALUES (#{memberId}, #{password}, #{name}, #{nickname}, #{email}, #{tel}, #{birthday}, '1', #{memberRole}, '1', sysdate)
	</insert>
	
	<update id="deleteMember" parameterType="String">
		UPDATE member
		SET status = '0'
		WHERE member_id = #{memberId}
	</update>
	
	<update id="updateLastLoginDate" parameterType="String">
		UPDATE member
		SET last_login_date = sysdate
		WHERE member_id = #{memberId}
	</update>
</mapper>